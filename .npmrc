# Allow native build scripts that the Cloudflare/wrangler stack needs.
auto-install-peers=true

# pnpm 10 requires explicit allow-list for postinstall scripts.
# Listed packages: workerd (Cloudflare runtime), esbuild (wrangler bundler),
# sharp (Cloudflare images, transitive). Keep this list minimal.
