{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SvelteKit frontend (customer + staff) for the booking system.",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "echo 'no tests in web app yet'",
    "test:coverage": "echo 'no tests in web app yet'"
  },
  "dependencies": {
    "@booking/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260504.0",
    "@inlang/paraglide-js": "^2.4.0",
    "@sveltejs/adapter-cloudflare": "^7.2.8",
    "@sveltejs/kit": "^2.59.1",
    "@sveltejs/vite-plugin-svelte": "^6.2.2",
    "@types/node": "^25.6.0",
    "graphql": "^16.13.2",
    "svelte": "^5.55.5",
    "svelte-check": "^4.4.5",
    "typescript": "^6.0.3",
    "vite": "^7.1.16"
  }
}
