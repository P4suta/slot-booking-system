{
  "name": "@booking/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Repository tooling — vitest runner, gate orchestrator, diagnose suite (Node-side TypeScript replacing legacy shell wrappers).",
  "devDependencies": {
    "@types/node": "*",
    "tsx": "*",
    "vitest": "*"
  }
}
